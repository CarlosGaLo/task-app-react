import React from 'react';
import TaskHeader from '../modules/TaskHeader';
import TaskFooter from '../modules/TaskFooter';

const Blackboard: React.FC = () => {
  return (
    <main>
        <TaskHeader/>

        <TaskFooter/>
    </main>
  );
};

export default Blackboard;